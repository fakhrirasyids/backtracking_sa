#include <iostream>
#include <vector>

using namespace std;

vector<int> V;

void tampil() {
    for (int i = 0; i < V.size(); i++) {
        cout << V[i] << endl;
    }
}

void add_tengah(int zzz, int xxx) {
    for (int x)
}

int main() {
    V.push_back(200);
    V.push_back(500);
    V.push_back(400);
    V.insert(V.begin(), 50);
    

    return 0;
}